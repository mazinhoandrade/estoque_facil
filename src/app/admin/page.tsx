import { Dashboard } from "@/src/components/dashboard";

export const Home = () => {
  return (
    <main className="min-h-screen items-center">
      <Dashboard />
    </main>
  );
};

export default Home;
